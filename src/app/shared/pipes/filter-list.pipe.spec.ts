import { FilterListBasedOnKeyValuePipe } from './filter-list.pipe';

describe('FilterListBasedOnKeyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListBasedOnKeyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
